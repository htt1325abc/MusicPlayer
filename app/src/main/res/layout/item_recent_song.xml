<?xml version="1.0" encoding="utf-8"?>
<!-- Item "Nghe gần đây" (list dọc): thumbnail TRÒN + tên bài + ca sĩ + nút play nhỏ -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp">

    <!-- Thumbnail tròn — dùng ShapeableImageView với shapeAppearanceOverlay Circle -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/ivRecentThumb"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:contentDescription="Ảnh bài hát"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_music_note"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Full" />

    <!-- Tên bài + ca sĩ -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvRecentTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textAppearance="?attr/textAppearanceTitleSmall"
            android:textColor="?attr/colorOnSurface"
            tools:text="Chạy Ngay Đi" />

        <TextView
            android:id="@+id/tvRecentArtist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textAppearance="?attr/textAppearanceBodySmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            tools:text="Sơn Tùng M-TP" />

    </LinearLayout>

    <!-- Nút play nhỏ bên phải -->
    <ImageButton
        android:id="@+id/btnRecentPlay"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Phát bài"
        android:scaleType="centerInside"
        android:src="@drawable/ic_play" />

</LinearLayout>
